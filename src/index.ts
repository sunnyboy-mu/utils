/**
 * @moe-cryo/utils
 *
 * =====================
 *
 * A common JavaScript utility library, zero dependencies, any runtime.
 *
 * 一个常用的工具类库, 无依赖， 任何运行时。
 *
 * ## Example
 *
 * ```ts
 * import { uniq, toArray } from '@moe-cryo/utils'
 *
 * uniq([1, 1, 2, 2, 3, 3]) // => [1, 2, 3]
 * toArray(null) // => []
 * toArray(2) // => [2]
 * ```
 *
 * @module
 */

export * from "./url";
export * from "./tree";
